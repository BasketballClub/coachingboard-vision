name: CI

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up Docker
        run: sudo sh ./install-docker.sh

      - name: Run tests
        run: sh ./run-tests.sh
